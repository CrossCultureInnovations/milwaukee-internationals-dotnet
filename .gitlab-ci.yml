include:
  - local: "ci/backup.gitlab-ci.yml"
  - local: "ci/deploy.gitlab-ci.yml"

stages:
  - deploy
  - backup

deploy:
  stage: deploy
  rules:
    - if: "$WHATEVER"
      when: never
  script:
    - echo "Running build"

backup:
  stage: backup
  rules:
    - if: "$WHATEVER"
  script:
    - echo "Running backup"
